fn sum(a, b) {
    return a + b;
}

fn main() {
    let foo;
    foo = 1;

    let bar = 1;

    /* TODO: poder usar [a, b, c, d, e] */
    let baz[32];

    let this_value;
    this_value = sum(1, 2);
}
